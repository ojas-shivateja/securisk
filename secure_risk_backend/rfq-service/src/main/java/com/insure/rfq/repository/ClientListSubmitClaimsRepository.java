package com.insure.rfq.repository;

import com.insure.rfq.entity.ClientListSubmitClaims;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClientListSubmitClaimsRepository extends JpaRepository<ClientListSubmitClaims,Long> {
}
